---
 - name: install on three servers
   become: yes
   hosts: webserver
   roles:
    - ngnix_role
 - name: install on three servers
   become: yes
   hosts: appserver
   roles:
    - nopcommerce_role
 - name: install on three servers
   become: yes
   hosts: dbserver
   roles:
    - geerlingguy.mysql
    
    
